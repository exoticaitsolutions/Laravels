$(document).ready(function(){
   // $('.baner_slide').slick();

   $('.baner_slide').slick({
    dots: true,
    infinite: true,
    speed: 300,
    slidesToShow: 1,
    centerMode: true,
    variableWidth: true
  });
});
